# PostgreSQL migration archive

这些迁移是迁移到 MySQL 之前的 PostgreSQL/PostGIS 历史记录，仅用于审计和数据迁移参考，不会被 Prisma 执行。

当前可执行迁移只有 `../migrations/20260814000000_mysql_baseline` 以及后续新增的 MySQL 迁移。不要把本目录的 SQL 文件复制回活动迁移目录。
